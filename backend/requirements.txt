fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.8
pillow>=10.0
httpx>=0.27
gymnasium>=0.29
stable-baselines3>=2.3
optuna>=3.6  # hyperparameter tuning
pybullet>=3.2.5  # physics + 3D render (a from-source build also works)
torch>=2.3  # standalone build replaces CPU Torch with the configured CUDA wheel
pytest>=8.0
httpx2>=2.0  # test client transport
mujoco>=3.2  # optional MuJoCo CPU preview/debug backend
jax>=0.4  # CPU JAX; install CUDA wheels separately for NVIDIA GPUs
brax>=0.10  # optional JAX-native PPO for MJX first-slice training
flax>=0.8
optax>=0.2
chex>=0.1
orbax-checkpoint>=0.6
