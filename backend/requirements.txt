fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.8
pillow>=10.0
httpx>=0.27
gymnasium>=0.29
stable-baselines3>=2.3
optuna>=3.6  # hyperparameter tuning
pybullet>=3.2.5  # physics + 3D render (a from-source build also works)
torch>=2.3  # CPU build is fine; pip on Windows installs CPU torch by default
pytest>=8.0
httpx2>=2.0  # test client transport
